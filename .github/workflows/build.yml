name: Build Solution Images (BE + FE) – Cached Base

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag prefix (e.g. v1.5, latest, dev)'
        required: true
        default: 'latest'

jobs:
  # -------------------------------------------------
  # 1. Discover solutions → output JSON array
  # -------------------------------------------------
  discover:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
        with:
          path: solutions

      - id: set
        run: |
          cd solutions
          items=$(find . -mindepth 1 -maxdepth 1 -type d | while read -r d; do
            folder=$(basename "$d")
            be=false; fe=false
            [ -f "$d/be-openimis.json" ] && be=true
            [ -f "$d/fe-openimis.json" ] && fe=true
            [[ $be == true || $fe == true ]] && \
              printf '{"solution":"%s","be":%s,"fe":%s},' "$folder" "$be" "$fe"
          done | sed 's/,$//')
          echo "matrix=[${items}]" >> $GITHUB_OUTPUT

  # -------------------------------------------------
  # 2. Build BASE images (once) – cached
  # -------------------------------------------------
  build-base:
    needs: discover
    runs-on: ubuntu-latest
    permissions:
      packages: write
    strategy:
      matrix:
        component: [be, fe]
    env:
      REPO: ${{ matrix.component == 'be' && 'openimis-be_py' || 'openimis-fe_js' }}
      BASE_TAG: ghcr.io/openimis/openimis-${{ matrix.component }}-base:${{ github.event.inputs.tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          repository: openimis/${{ env.REPO }}
          ref: develop
          path: base-repo

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push BASE image (cached)
        uses: docker/build-push-action@v5
        with:
          context: base-repo
          target: base
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.BASE_TAG }}
          cache-from: type=gha,scope=base-${{ matrix.component }}
          cache-to: type=gha,mode=max,scope=base-${{ matrix.component }}
          build-args: |
            OPENIMIS_CONF_JSON={}

      # Export tag using bash
      - name: Export base tag
        run: |
          COMP=$(echo "${{ matrix.component }}" | tr '[:lower:]' '[:upper:]')
          echo "BASE_${COMP}_TAG=${{ env.BASE_TAG }}" >> $GITHUB_ENV

      # Expose as job output
      - name: Set job output
        id: out
        run: |
          COMP=$(echo "${{ matrix.component }}" | tr '[:lower:]' '[:upper:]')
          echo "BASE_${COMP}_TAG=${{ env.BASE_TAG }}" >> $GITHUB_OUTPUT

    outputs:
      BASE_BE_TAG: ${{ steps.out.outputs.BASE_BE_TAG }}
      BASE_FE_TAG: ${{ steps.out.outputs.BASE_FE_TAG }}

  # -------------------------------------------------
  # 3. Build solution images – reuse base + compact JSON
  # -------------------------------------------------
  build-solution:
    needs: [discover, build-base]
    runs-on: ubuntu-latest
    permissions:
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.discover.outputs.matrix) }}   # ← FIXED
    env:
      TAG_PREFIX: ${{ github.event.inputs.tag }}
      BASE_BE: ${{ needs.build-base.outputs.BASE_BE_TAG }}
      BASE_FE: ${{ needs.build-base.outputs.BASE_FE_TAG }}

    steps:
      - uses: actions/checkout@v4
        with:
          path: solutions

      # Install jq (for compacting JSON)
      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Copy core Dockerfiles into solution folder
      - name: Copy core Dockerfiles
        run: |
          mkdir -p solutions/${{ matrix.solution }}/core/be
          mkdir -p solutions/${{ matrix.solution }}/core/fe
          cp -r base-repo-be/* solutions/${{ matrix.solution }}/core/be/ 2>/dev/null || true
          cp -r base-repo-fe/* solutions/${{ matrix.solution }}/core/fe/ 2>/dev/null || true

      # ---------- BE ----------
      - name: Build BE image
        if: matrix.be
        uses: docker/build-push-action@v5
        with:
          context: solutions/${{ matrix.solution }}
          file: core/be/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/openimis/${{ env.TAG_PREFIX }}-${{ matrix.solution }}-be
          build-args: |
            OPENIMIS_CONF_JSON=$(jq -c . solutions/${{ matrix.solution }}/be-openimis.json)
          cache-from: |
            type=gha,scope=base-be
            ${{ env.BASE_BE }}
          cache-to: type=gha,mode=max,scope=solution-${{ matrix.solution }}-be

      # ---------- FE ----------
      - name: Build FE image
        if: matrix.fe
        uses: docker/build-push-action@v5
        with:
          context: solutions/${{ matrix.solution }}
          file: core/fe/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/openimis/${{ env.TAG_PREFIX }}-${{ matrix.solution }}-fe
          build-args: |
            OPENIMIS_CONF_JSON=$(jq -c . solutions/${{ matrix.solution }}/fe-openimis.json)
          cache-from: |
            type=gha,scope=base-fe
            ${{ env.BASE_FE }}
          cache-to: type=gha,mode=max,scope=solution-${{ matrix.solution }}-fe
